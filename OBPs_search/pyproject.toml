[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "obps-search"
version = "0.3.0"
description = "OBP candidate ranking from VOC affinity data"
requires-python = ">=3.10"

[project.scripts]
obps-search = "obps_search.cli:main"
